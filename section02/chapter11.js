// 동기 : 여러개의 작업을 순서대로, 하나씩 진행
// 자바스크립트에는 멀티쓰레드가 없다. 싱글스레드이다.
// 비동기 : 동기적이지 않다는 뜻으로, 작업을 순서대로 처리하지 않음

console.log(1);
setTimeout(() => {
  console.log(2);
}, 3000);
console.log(3);

// 비동기 작업들은 자바스크립트 엔진이 아닌 Web APIs에서 실행됨
