// 1. Number Type

let num1 = 27;
let num2 = 1.5;
let num3 = -20;

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN;

// 2. String type
let myName = "김민혁";
let myLocation = "태평";
let introduce = myName + myLocation;
let introduceText = `${myName}은 ${myLocation}에 거주합니다.`;
// ``안에는 동적으로 변수명을 사용하여 문자열을 만들 수 있다.
// 템플릿 리터럴 문법

// 3. Boolean Type
let isTrue = true;
let isFalse = false;

// 4. Null Type ( 아무것도 없다. )
let empty = null;

// 5. Undefined Type
let none;

// Null은 null을 의도해서 넣은 것이고, Undefined는 정말 정의되지 않았다는 것.
